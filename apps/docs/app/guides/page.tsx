import { GuidesIndexContent } from '@/components/GuidesIndexContent';

export const metadata = { title: 'Guides' };

export default function GuidesIndexPage(): React.ReactElement {
  return <GuidesIndexContent />;
}
