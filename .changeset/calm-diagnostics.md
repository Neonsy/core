---
'@fluxerjs/core': minor
'@fluxerjs/rest': minor
'@fluxerjs/ws': minor
'@fluxerjs/voice': minor
---

Add opt-in structured diagnostics with bounded support reports for core, REST,
gateway, voice, and clustered clients. Voice diagnostics replace the
`VOICE_DEBUG` console logger and are enabled through the client.
